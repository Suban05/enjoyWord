<%= search_form_for @q, data: { turbo_frame: 'list' } do |f| %>
    <div class="d-flex">
        <%= hidden_field_tag :dictionary_id, @dictionary.id %>
        <%= f.label :content_or_translation_cont %>
        <%= f.search_field :content_or_translation_cont, oninput: 'this.form.requestSubmit()' %>
    </div>
<% end %>
