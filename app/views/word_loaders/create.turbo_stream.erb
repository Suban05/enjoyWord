<%= turbo_stream.replace Word.new,
    partial: 'shared/header',
    locals: {
        list_title_path: @dictionary.name,
        create_button_title_path: t('word.index.new_word'),
        create_button_link: new_word_path(dictionary_id: @dictionary),
        load_words_button_link: new_word_loader_path(dictionary_id: @dictionary),
        new_object: Word.new
     }
%>
<% @words.each do |word| %>
    <%= turbo_stream.append 'list', word %>
<% end %>
<%= prepend_flash %>
